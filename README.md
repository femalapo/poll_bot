Discord bot to tally unique practice reactions to polls across multiple channels.
Use the .txt files to configure the bot and the poll.

bot_config.txt -> bot token and channels to post poll/results in
poll_config.txt -> configurations for the poll itself
